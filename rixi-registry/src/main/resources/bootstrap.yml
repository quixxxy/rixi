spring:
  application:
    name: rixi-registry
  cloud:
    config:
      uri: http://rixi-config:8888
      fail-fast: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0
